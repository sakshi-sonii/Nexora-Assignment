import { AnyAction } from 'redux';

const initialState = {};

const rootReducer = (state = initialState, _action: AnyAction) => {
    // simple placeholder reducer; extend as needed
    return state;
};

export default rootReducer;
